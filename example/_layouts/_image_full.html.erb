<% if data(:image) %>
  <figure class="post-image-figure">
    <%= img data(:image).orig.url,
      class: "post-image img-no-border img_full" %>
    <% unless data(:caption).empty? %>
      <figcaption class="post-image-hint">
        <%= data(:caption) %> 
      </figcaption>
    <% end %>
  </figure>
<% end %>
